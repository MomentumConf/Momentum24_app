{
    "buildCommand": "flutter build web --wasm --dart-define-from-file=.env --pwa-strategy=none --release",
    "outputDirectory": "build/web/",
    "installCommand": "flutter pub get",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}