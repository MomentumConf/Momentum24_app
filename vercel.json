{
    "buildCommand": "flutter build web",
    "outputDirectory": "build/web/",
    "installCommand": "flutter pub get",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}